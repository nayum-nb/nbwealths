# nbwealths

Wealth management app monorepo — scaffold for learning and interview preparation.

This repository is generated by GitHub Copilot assistant. It contains three Spring Boot services (Account, Portfolio, Admin), an Angular frontend scaffold, local dev docker-compose (Postgres ×3, Kafka, Zookeeper, Keycloak), Terraform placeholders, and a GitHub Actions CI template.

Next steps:
- Branch scaffold/maven-java17-keycloak-kafka will contain the initial scaffold files.
- Run local dev with docker-compose (see infra/docker-compose.yml in the scaffold branch).

See the scaffold branch for the project layout and files.
