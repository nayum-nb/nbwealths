server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/${POSTGRES_ADMIN_DB}
    username: ${POSTGRES_ADMIN_USER}
    password: ${POSTGRES_ADMIN_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
