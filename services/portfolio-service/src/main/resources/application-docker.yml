server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://postgres-portfolio:5432/${POSTGRES_PORTFOLIO_DB}
    username: ${POSTGRES_PORTFOLIO_USER}
    password: ${POSTGRES_PORTFOLIO_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

kafka:
  bootstrap-servers: kafka:9092
